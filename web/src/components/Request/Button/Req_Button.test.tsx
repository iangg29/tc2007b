// (c) Tecnologico de Monterrey 2022, rights reserved.
// import { render, screen } from "@testing-library/react";
// import ReqButton from "./Req_Button";
// import { BrowserRouter as Router } from "react-router-dom";

// Generates API requests and marks it as an error
describe("Test ReqButton component", () => {
  it("render component", () => {
    // render(
    //   <Router>
    //     <ReqButton text="Botón prueba" navigate="/app/home" appID="1" next={2} />
    //   </Router>,
    // );
  });
});

// beforeEach(() => render(<ReqButton text="Botón prueba" navigate="/app/home" appID="1" next={2} />));

// test("ReqButton text param validation", () => {
//   const btnEl = screen.getByRole("button", { name: /Botón prueba/i });
//   expect(btnEl).toBeInTheDocument();
// });
